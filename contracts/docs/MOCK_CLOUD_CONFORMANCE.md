# Mock Cloud Conformance Guide

Mock Cloud simulates Pollek Cloud for offline / local integration testing.

- It must consume generated artifacts (OpenAPI, JSON Schema).
- It must not use independent logic for schema validation.
- Implement tests to ensure mock behavior strictly matches provider requirements.
